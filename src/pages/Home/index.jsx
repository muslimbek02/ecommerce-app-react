import { Button } from "@mui/material";

const Home = () => {
  return (
    <div>
      <Button
        variant="contained"
        sx={{ color: "#fff", background: "#42D1FF", }}
      >
        Contained
      </Button>
    </div>
  );
};

export default Home;
